import { modelfield } from '../../../helpers/modelfield';
import { module, test } from 'qunit';

module('Unit | Helper | modelfield');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = modelfield(42);
  assert.ok(result);
});
